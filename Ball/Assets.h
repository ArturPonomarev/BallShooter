#pragma once

enum Fonts
{
	MAIN_FONT,
};

enum Textures
{
	MARK_TEXTURE,
	CANNON_TEXTURE,
};

enum Alliance
{
	NONE_ALLIANCE,
	ENEMY_ALLIANCE,
	ALLY_ALLIANCE,
};

enum ObjectTypes
{
	NONE_TYPE,
	PLAYER_TYPE,
	BULLET_TYPE,
	ENEMY_TYPE,
};

